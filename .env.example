# ============================================================
# Lead Management System — Environment Variables
# Copy this file to .env.local and fill in your values.
# ============================================================

# === FRONTEND (Vite — must be prefixed with VITE_) ===

# Supabase project URL and anonymous key (from Supabase dashboard > Settings > API)
VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Google OAuth Client ID (from Google Cloud Console > APIs & Services > Credentials)
VITE_GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com

# n8n Deep Research webhook URL (optional — triggers AI lead research)
VITE_DEEP_RESEARCH_WEBHOOK_URL=https://your-n8n-instance.com/webhook/deep-research

# Branding — company name shown in sidebar, login page, loading screen
VITE_COMPANY_NAME=YourCompany

# Optional — company logo URL (if blank, shows first letter of company name)
VITE_COMPANY_LOGO_URL=


# ============================================================
# SUPABASE EDGE FUNCTION SECRETS
# Set these via: npx supabase secrets set KEY=VALUE
# Do NOT put these in .env.local (they're backend-only)
# ============================================================

# Google OAuth (same Client ID as above, plus the secret)
# GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=your_google_client_secret

# Supabase service role key (from dashboard > Settings > API > service_role)
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# Google Gemini API key (for AI email classification in gmail-webhook)
# GEMINI_API_KEY=your_gemini_api_key

# Google Cloud project ID (for Gmail Push Notifications via Pub/Sub)
# GCP_PROJECT_ID=your-gcp-project-id

# IANA timezone for calendar events (e.g., America/New_York, Europe/London, Australia/Brisbane)
# TIMEZONE=UTC

# Company name used in email templates (header + footer)
# COMPANY_NAME=YourCompany

# Comma-separated email domains to auto-dismiss in gmail-webhook
# BLOCKED_EMAIL_DOMAINS=mailchimp.com,sendgrid.net,noreply.google.com

# n8n webhook URLs (optional — for prospect reply tracking and form submissions)
# N8N_PROSPECT_REPLY_WEBHOOK_URL=https://your-n8n.com/webhook/prospect-reply
# N8N_FORM_SUBMISSION_WEBHOOK_URL=https://your-n8n.com/webhook/form-submission

# Secret for validating form webhook requests
# FORM_WEBHOOK_SECRET=generate_a_random_secret_here
